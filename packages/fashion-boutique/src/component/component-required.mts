/**
 * Represents a component that has a required property.
 */
export interface IZComponentRequired {
  /**
   * Specifies that interaction with the target element is required before moving forward.
   */
  required?: boolean;
}
