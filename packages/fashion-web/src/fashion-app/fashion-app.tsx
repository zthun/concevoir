import React from 'react';

/**
 * Represents the root entry point into the application.
 *
 * @returns
 *        The jsx to render the fashion web application.
 */
export function ZFashionApp() {
  return <main className='ZFashionApp-root'>Fashion!</main>;
}
